export interface Product {
    partNumber: number;
    series: string;
    dualSize: string;
    pressureRange: string;
    connections: string;
    connectionLoc: string;
    accuracy: string;
    caseMaterial: string;
    weightingParts: string;
    fill: string;
}